# Slack Bot
SLACK_BOT_TOKEN=xoxb-...
SLACK_SIGNING_SECRET=...
SLACK_SIMON_DM_CHANNEL=D...    # Simon's DM channel ID with the bot (found after bot install)
SLACK_SAMU_CHANNEL=C...        # Channel where EOD checkout gets posted (e.g. #project-management)
SLACK_USER_TOKEN=xoxp-...      # User token for reading private channels (existing)

# Agent behavior
AGENT_WRITES_ENABLED=true      # Set false to disable all Airtable writes (read-only mode)

# Anthropic
ANTHROPIC_API_KEY=sk-ant-...

# Airtable
AIRTABLE_API_KEY=pat...
AIRTABLE_BASE_ID=app...

# Railway
PORT=3000                      # Railway sets this automatically; default 3000
